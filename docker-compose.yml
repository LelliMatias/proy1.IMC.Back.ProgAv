version: '3.9'
services:
  mongo:
    image: mongo:8.0.12
    container_name: mongo-db
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: imc_db
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
